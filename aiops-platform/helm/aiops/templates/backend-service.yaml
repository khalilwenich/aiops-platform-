apiVersion: v1
kind: Service
metadata:
  name: aiops-backend
spec:
  selector:
    app: aiops-backend
  ports:
    - name: http
      port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}
